export default function Page() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif" }}>
      <header style={{ padding: "24px 32px", borderBottom: "1px solid #eef0f3" }}>
        <div style={{ width: 26, height: 26, borderRadius: 7, background: "{{COR_ACENTO}}" }} />
        <span style={{ fontWeight: 700, marginLeft: 8 }}>{{MARCA}}</span>
      </header>
      <section style={{ padding: "64px 32px" }}>
        <h1>{{MARCA}}</h1>
      </section>
    </main>
  );
}
